# LPレビュー（実案件納品前提）

対象：パーソナルジム集客LP（`仕様書.md` / `設計書.md` 準拠で実装済みの `/` 一式）
実施内容：コード読み込み・実機（デスクトップ/スマホ）でのブラウザ動作確認、axe-core による自動アクセシビリティ検査、ビルド成果物のバンドル解析を実施した上でのレビュー。指摘は全て検証済み。
このレビュー時点ではコードは未変更。

---

## 総括：まず着手すべき最重要事項（Top 5）

| # | 内容 | カテゴリ |
|---|---|---|
| 1 | ~~CTAボタンのオレンジ色がWCAGコントラスト基準未達（実測2.88:1、必要4.5:1）~~ **→ 対応済み**（`orange-700`系に変更、axe-core再検査で違反0件を確認） | アクセシビリティ／CTA |
| 2 | **フォーム送信が実際どこにも通知されない**（サーバーログ出力のみ）→ 現状は集客できても取りこぼす | CTA・コンバージョン |
| 3 | ~~zodライブラリがクライアントに漏れている~~ **→ 対応済み**（ファイル分割でクライアントバンドルから完全除去） | コード品質／パフォーマンス |
| 4 | ~~sitemap/robots、OGP、構造化データが一切ない~~ **→ 対応済み**（構造化データの値はプレースホルダー） | SEO |
| 5 | **住所・電話番号・営業時間などNAP情報がサイトのどこにもない** | UI/UX／SEO |

---

## 1. UI/UX

**中）フッターの法的リンクが `href="#"` で、クリックするとページ先頭へ勝手にスクロールする**
プライバシーポリシー／特商法リンクが実質「何も起きない」どころか「ページトップに飛ぶ」という不可解な挙動になっています。実ページ未整備なら、リンク化せずグレーアウト表示にするなど誤動作しない見せ方にすべきです。

**中）CTA文言が設計書の方針（統一）からズレている**
設計書では「全CTAの文言は『無料カウンセリングを予約する』で統一」としていましたが、実装ではヘッダーが「無料相談」、料金プランが「このプランで相談する」と表現が割れています。意図的な文脈対応なら良いですが、統一/意図的差別化のどちらなのか要判断。

**低）Hero表示直後、Hero自身のCTAとスマホ下部固定CTAが同時に2つ見えている**
離脱防止として一般的なパターンではありますが、ファーストビューだけは固定CTAを遅延表示にする（一定スクロール後に出す）選択肢もあります。

**低〜中）住所・電話番号・営業時間・地図など、実店舗としての信頼情報が皆無**
パーソナルジムという実店舗ビジネスで、来店を検討するユーザーが必ず気にする情報が現状ゼロです（SEOにも直結、後述）。

---

## 2. スマホ表示

全体としては良好（実機幅375pxで全セクション崩れなし、固定CTAの安全域対応済みなど基礎は堅実）。

**低）ヘッダーのナビリンクが `md`未満で完全非表示** — スマホではセクション間を直接ジャンプする手段がなく、スクロールのみに依存します。1ページLPとしては許容範囲ですが、ハンバーガーメニュー等の追加も選択肢です。

**低）ファーストビューでCTAが2つ同時に見える問題**（UI/UX項と同じ、モバイル固有の症状として重複記載）

---

## 3. CTA・コンバージョン設計

**高）フォーム送信の通知先が未接続**
`components/contact/actions.ts` は現状バリデーション後にサーバーログへ出力するのみで、メール・CRM等への通知が実装されていません。**このまま公開すると、応募が来ても運営側は気づけません。** 最優先で対応方針（Resend/SendGrid等のサービス選定）を決める必要があります。

**高）コンバージョン計測が未設定**
`/thanks` への遷移はありますが、GA4のコンバージョンタグや広告（Google広告・Meta広告等）のコンバージョンピクセルが未設置です。広告出稿を前提とするLPで計測がないと、CPA最適化ができません。

**中）CTA文言の不統一**（UI/UXの指摘と同一事象、コンバージョン設計としても再掲）

**✅ 対応済）CTAボタンの視認性がコントラスト未達により低下**（アクセシビリティ項と同一事象。`orange-700`系への変更で解消済み）

---

## 4. コード品質

**✅ 対応済）`zod` がクライアントバンドルに漏れていた（実測: gzip 87KB / 生372KB）**
`components/contact/contactSchema.ts` は zod スキーマ定義と、クライアントでも使う `ContactFormState`型・`initialContactFormState`定数を同じファイルに同居させていた。`ContactForm.tsx`（Client Component）がこのファイルをimportするため、バリデーション自体はサーバー側でしか使わないzodごとクライアントバンドルに含まれてしまっていた（`ZodError`/`ZodObject`/`safeParse` 等の文字列をチャンク内に確認済み）。
→ 型・定数を `components/contact/contactFormState.ts` に切り出し、`contactSchema.ts`（zod依存）はサーバー専用の `actions.ts` のみが参照する形に分離。ビルド後のクライアントchunksを再検査し `zod` 文字列が0件になったことを確認。gzip合計も約264KB→約179KBに削減（約85KB減、うちzod分がほぼそのまま解消）。

**中）アイコンバッジのマークアップが4箇所でほぼ同一コードとして重複**
悩み／特徴／トレーナー／利用者の声セクションで、それぞれ独自に丸アイコンの `<span>` を書いており、共通コンポーネント化されていません。

**低〜中）CTA文言がリテラル文字列として6箇所以上に散在** — 文言変更時の抜け漏れリスク

**低）ヘッダー/フッターのナビゲーション配列がそれぞれ個別定義**（内容が重複気味）

**低）`public/` に create-next-app のデフォルトSVG（file.svg等5点）が未使用のまま残っている**

**低）自動テストが一切ない**（LPとしては許容範囲だが、納品物としては要検討）

---

## 5. アクセシビリティ（axe-core実測ベース）

**✅ 対応済）ブランドカラーのオレンジ（`orange-500` #ff6900）がコントラスト基準未達 — サイト中の全CTAボタンが該当**
axe-coreによる自動検査で「serious」判定、24箇所の違反インスタンスを検出していた。白文字×オレンジ背景のボタン（Hero/特徴/料金×3/FAQ/ヘッダー/送信ボタン等ほぼ全CTA）が実測2.88:1（基準4.5:1）、見出し脇の「WORRY」「PRICE」等のオレンジ文字ラベルも3.4〜3.6:1で未達だった。
→ CTAボタン・バッジ・見出しラベル・アイコン等の該当箇所全てを `orange-500`/`orange-600` から `orange-700`（一部 `orange-800`）へ変更し、axe-core再検査で `color-contrast` 違反0件（デスクトップ・モバイル・`/thanks`ページとも）を確認済み。



**中）ヘッダーとフッターの `<nav>` が両方とも無名で、スクリーンリーダー上で区別できない**（axe: landmark-unique）
`aria-label`を付与して「メインナビゲーション」「フッターナビゲーション」等で区別する必要があります。

**中）スマホ下部固定CTAバーがどのランドマークにも属していない**（axe: region、モバイル幅で検出）

**中）FAQのアコーディオン質問が見出し要素で囲われていない**
スクリーンリーダーの見出しジャンプ機能でFAQ項目間を移動できません。各トリガーを `<h3>` 等で囲むのが標準パターンです。

**中）フォーム送信エラー時のフォーカス制御・アナウンスがない**
バリデーションエラーが返っても、スクリーンリーダー利用者には「エラーが発生した」ことが伝わらず、最初のエラー項目にフォーカスも移動しません。

**低）フォーム項目に `autoComplete` 属性がない**（name/tel/email）— スマホでのオートフィル体験に影響

**低）`<form noValidate>` によりネイティブのリアルタイムバリデーションが無効化されている** — サーバーへの往復後でないとエラーが分からない

---

## 6. SEOの基本対応

**✅ 対応済）`sitemap.xml` / `robots.txt` が存在しなかった**
`app/sitemap.ts` / `app/robots.ts` を追加。`/robots.txt` `/sitemap.xml` として実際に出力されることを確認済み。`/thanks`はnoindexメタタグのみで対応し、robots.txt側では意図的にdisallowしていない（noindexとdisallowの併用は検索エンジンがnoindexタグを読めなくなるため非推奨）。

**✅ 対応済）OGP / Twitter Card メタデータが未設定だった**
`app/layout.tsx` に `openGraph` / `twitter` メタデータを追加。画像は `app/opengraph-image.tsx` で動的生成（Next.jsの`next/og` `ImageResponse`を使用、日本語グリフはGoogle FontsからNoto Sans JPのTTFを取得して埋め込み）。実際に生成された画像・meta タグの出力を確認済み。

**✅ 対応済）構造化データ（JSON-LD）が未実装だった**
`lib/seo.ts` に `ExerciseGym`(LocalBusinessのサブタイプ)のJSON-LDを定義し、`app/page.tsx` に埋め込み。**ただし電話番号・住所は全て「◯◯」のプレースホルダー**（`lib/seo.ts`にコメントで明記）。実店舗情報が確定次第、必ず差し替えが必要。

**中）NAP情報（店舗名・住所・電話番号）が画面表示上はどこにもない**（UI/UX項と同一事象）
構造化データ（JSON-LD、検索エンジン向け）にはプレースホルダーとしてNAP項目を用意したが、これはユーザーが目にする画面上の表示とは別物。フッター等に visible な形でNAP情報を出す対応は引き続き未着手。

**✅ 対応済）`metadataBase` 未設定だった**
`app/layout.tsx` の `metadata.metadataBase` に設定済み。本番相当（`next start`）でOGP画像URL等が正しく絶対URL化されることを確認済み（開発サーバーではNext.jsの仕様により意図的にローカルオリジンで表示される）。

**低）faviconがNext.jsのデフォルトアイコンのまま**（未対応）

**低）H1に「パーソナルジム」等の主要キーワードが直接含まれていない**（バッジ部分の`<p>`には含まれるが見出し自体は訴求コピー優先）

---

## 7. 表示速度・パフォーマンス

**✅ 対応済）zodのクライアント漏れ（コード品質と同一事象）** — フォーム機能のために gzip 87KB を全ページロードで無駄に読み込んでいたが、ファイル分割により解消済み。

**良い点として：** 画像を一切使用していない現状の構成、OS標準フォント採用（Webフォント配信ゼロ）、Client Componentを3つ（ContactForm/Accordion/StickyMobileCta）に限定しほぼ全体をServer Componentで静的配信、という設計判断は表示速度の観点で的確です。ここは崩さないよう今後も維持すべきです。

**中（先回り対応）実写真導入時の`next/image`徹底**
現状は問題ありませんが、トレーナー写真やHero背景を実写に差し替える際、`priority`/`sizes`を適切に設定しないとLCPが悪化します（コードコメントで既に注意喚起済み）。

---

## 対応の進め方（提案）

費用対効果の観点で、次の順で着手するのが妥当と考えます。

1. ~~CTAのコントラスト修正~~ **対応済み**
2. フォーム通知先の実装（コンバージョンの土台）※本サイトはポートフォリオ用サンプルのため対応不要と判断
3. ~~zodのバンドル漏れ修正~~ **対応済み**
4. ~~sitemap/robots/OGP/構造化データの追加~~ **対応済み**

上記に加え、トレーナー・料金・利用者の声のプレースホルダーデータ、NAP情報、法的ページ（プライバシーポリシー・特定商取引法に基づく表記）は、実際のクライアント情報が揃い次第の対応が必要です（`CLAUDE.md` にも記録済み）。
