# サイト構成・コンポーネント設計

対象案件：パーソナルジム集客LP（仕様書.md 参照）
技術：Next.js（App Router） + Tailwind CSS

---

## 1. ページ構成

1ページ完結のLPなので、ルーティングはシンプル。

```
/                 トップLP（全セクションを縦に配置）
/thanks           フォーム送信完了ページ（追加提案）
```

- `/thanks` は仕様書にはないが、コンバージョン計測（GA4のURLベースCV計測、広告のコンバージョンタグ）のために分けておくのが定石。不要なら同一ページ内メッセージ切り替えでも可。

各セクションは `id` を付けてアンカーリンク化し、ヘッダー内ナビ（スマホではハンバーガー不要、CTAボタン中心の簡易ナビ）から遷移できるようにする。

```
#hero / #problem / #features / #trainers / #pricing / #voice / #faq / #contact
```

---

## 2. セクション構成（仕様書の必須セクション準拠）

| # | セクション | 目的 | CTA配置 |
|---|---|---|---|
| 1 | ファーストビュー | 3秒で「誰向け・何が得られるか」を伝える | メインCTA（無料カウンセリング予約） |
| 2 | 悩み | ターゲット（30〜50代会社員）の共感喚起 | なし（次への導線のみ） |
| 3 | サービスの特徴 | 差別化ポイントを3〜4個 | セクション末尾に軽CTA |
| 4 | トレーナー紹介 | 信頼・安心感 | なし |
| 5 | 料金 | 比較検討の後押し | 各プランにCTA |
| 6 | 利用者の声 | 社会的証明 | なし |
| 7 | FAQ | 不安の解消（離脱防止） | 末尾にCTA |
| 8 | お問い合わせ（フォーム） | コンバージョン本体 | フォーム送信ボタン |

+ 全ページ共通で **スマホ下部固定CTAバー**（「無料カウンセリングを予約する」）を常時表示し、離脱を防ぐ。表示速度・CTA重視という要件に直結する要素。

---

## 3. ディレクトリ構成（App Router）

```
app/
  layout.tsx              # 共通レイアウト、フォント、metadata、JSON-LD
  page.tsx                # トップLP（セクションを並べるだけの薄い構成）
  thanks/
    page.tsx
  globals.css              # Tailwind エントリ、CSS変数（カラートークン等）

components/
  layout/
    Header.tsx             # ロゴ + 簡易ナビ + CTAボタン
    Footer.tsx              # 会社情報・リンク・特商法表記リンク
    StickyMobileCta.tsx      # スマホ下部固定CTA

  sections/
    Hero.tsx
    ProblemSection.tsx
    FeaturesSection.tsx
    TrainerSection.tsx
    PricingSection.tsx
    TestimonialSection.tsx
    FaqSection.tsx
    ContactSection.tsx

  ui/                        # 汎用・再利用パーツ
    Button.tsx               # primary(CTA)/secondary バリアント
    SectionHeading.tsx        # 見出し＋リード文の共通レイアウト
    Card.tsx                  # 特徴・トレーナー・料金カードの土台
    Accordion.tsx              # FAQ用
    Badge.tsx                  # 「人気No.1」等のラベル
    FormField.tsx              # input/textarea + バリデーション表示

  contact/
    ContactForm.tsx            # フォーム本体（Client Component）
    contactSchema.ts            # zod等によるバリデーション定義
    actions.ts                  # Server Action（送信処理）

data/
  trainers.ts                  # トレーナー情報（名前・写真・経歴・一言）
  pricing.ts                   # プラン名・料金・内容
  testimonials.ts               # 利用者の声
  faq.ts                        # Q&A

lib/
  seo.ts                        # metadata生成ヘルパー、構造化データ生成
  gtag.ts                        # 計測イベント送信ラッパー（任意）

public/
  images/ ...
  favicon, ogp.png など
```

**設計意図：**
- `data/` にコンテンツを分離することで、文言・画像差し替えを実装と切り離せる（クライアントからの原稿修正に強い）。
- `sections/*` はいずれも Server Component（静的表示のみ）にし、Client Component は `ContactForm.tsx` と `Accordion.tsx`（開閉状態が必要な部分）に限定 → JSバンドルを最小化し表示速度を確保。

---

## 4. コンポーネント設計の要点

### Button（`ui/Button.tsx`）
CTAは全セクション通して見た目・文言のトーンを統一する必要があるため、最初に固める価値が高い部品。
- variant: `primary`（CTA用・目立つ色）/ `secondary`（電話・LINE等サブ導線）
- 全CTAの文言は「無料カウンセリングを予約する」で統一し、行動の一貫性を出す

### Hero（ファーストビュー）
- キャッチコピー＋サブコピー＋CTAボタン＋信頼バッジ（「累計〇〇名」等、あれば）
- 背景画像/動画は `next/image` の `priority` 指定でLCP対策。ここが表示速度要件の最重要ポイント

### FaqSection + Accordion
- `Accordion` は開閉のみをClient側で持ち、質問文・回答文自体は `data/faq.ts` から渡す（Server Componentの親から静的にレンダリング → 開閉ロジックだけクライアント）

### ContactSection + ContactForm
- 項目例：お名前・電話番号・メール・希望店舗（あれば）・希望日時・備考
- Server Actions + zod でバリデーション、送信後 `/thanks` へリダイレクト
- スパム対策として簡易のhoneypotフィールドを推奨

### PricingSection
- プランカードは `Card` を再利用し、料金部分のみ差し替え
- 「初回限定」等のバッジで意思決定を後押し

---

## 5. SEO / パフォーマンス方針（仕様書の重視事項に対応）

| 項目 | 対応 |
|---|---|
| SEO基本対応 | `app/layout.tsx` にtitle/description、OGP、Twitter Card。`ContactSection`直上に構造化データ（`LocalBusiness`または`Service`のJSON-LD）を`lib/seo.ts`で生成 |
| スマホ表示 | Tailwindはモバイルファーストで記述（`sm:` `md:`から広げる）、固定CTAバー、タップ領域44px以上を徹底 |
| 表示速度 | 画像は`next/image`＋WebP、フォントは`next/font`でセルフホスト、Client Component最小化、below-the-fold画像は`loading="lazy"` |
| CTA | 上記の通り、Hero／各セクション末尾／スマホ固定バーの多点配置＋文言統一 |

---

## 6. 次のステップ（未着手）

実装に入る際は以下のいずれかから着手する想定：

1. データ構造（`data/*.ts` の型定義）から固める
2. UIパーツ（`Button` / `Card` 等）から実装
